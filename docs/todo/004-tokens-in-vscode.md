seems like tokens in/out are not updated in vscode extention the placeholder shows n/a all the time. 
also update Duration every 0.1s regardless on events. 

after cancelling scan for a moment status shows scan cancelled but after o.5s goes back to "Running bp-arch-003-composition-root-owns-wiring"

after cancelling scan with zero checks completed the panel shows "25 total checks scanned"

