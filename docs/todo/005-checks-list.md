Remove the header from checks list: "Checks (25) Sort: Status"
Stretch the list to 100% width of the container.
The container has already title "CHECKS" add number of checks to the container name so it will become "CHECKS (25)"